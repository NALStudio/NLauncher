<MudIconButton Class="@Class"
               Style="@(GetStyleCss())"
               Icon="@(GetIcon())"
               Color="@(GetColor())"
               Size="Size.Large"
               Variant="Variant.Filled"
               DropShadow="false"
               OnClick="@Activate"
               Href="@PlayHref"
               Target="_blank" />